# Made with React + TypeScript + Vite

## Run

1. Add your OpenWeather api in `.env.example` file and rename it to `.env`

2. Install all packages with `npm i`

3. Run

```bash
npx vite
#or
npm run dev
```
